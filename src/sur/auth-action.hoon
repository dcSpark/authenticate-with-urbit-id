|%
+$  action
  $%  [%generate-token target=ship]  :: generate unique token at some strength
      [%reset-token target=ship]     :: reset the assigned token
      [%send-token target=ship]      :: send the token to the target ship
      ::[%receive-token]               :: currently unnecessary
      ::[%write-token]                 :: trigger %clay output later
  ==
--
